---
description: Run the LCBP3 bugfix workflow (root-cause first, tiered verification)
---

Follow `.claude/skills/bugfix/SKILL.md` exactly. Restate the bug in unambiguous terms first (per `AGENTS.md` Analysis Phase), identify the relevant spec/ADR from `.devin/rules/12-key-spec-files.md`, then plan before editing. Bug: $ARGUMENTS
