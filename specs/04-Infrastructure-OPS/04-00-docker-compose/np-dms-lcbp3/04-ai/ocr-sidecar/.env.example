# File: specs/04-Infrastructure-OPS/04-00-docker-compose/np-dms-lcbp3/04-ai/ocr-sidecar/.env.example
# OCR Sidecar environment variables — รันบน New Server (192.168.10.11) ตาม ADR-041
# คัดลอกไฟล์นี้เป็น .env แล้วกรอกค่าจริง
# ห้าม commit ไฟล์ .env ขึ้น git
# Change Log:
# - 2026-07-30: ADR-040 Phase 2 (T016) — ลบ OCR_SIDECAR_API_KEY (network isolation แทน X-API-Key auth)

# OCR Sidecar storage boundary (ADR-040 D7)
OCR_SIDECAR_UPLOAD_BASE=/mnt/uploads
